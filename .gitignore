# ───────────────────────── OS / Python ─────────────────────────
.DS_Store
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.coverage
.venv/
.env
.env.*
*.log

# ───────────────────────── Secrets (NEVER commit) ─────────────────────────
*_key
voyage_key
gemini_key*
*.pem

# ───────────────────────── Large data / corpus (local & VM only) ─────────────────────────
data/
*.db
*.db-shm
*.db-wal
*.jsonl.gz
# big source/derived JSONL kept out of git (rebuild via scripts; publish curated sets to HF)
echr_cases*.jsonl
echr_decisions*.jsonl
echr_cases_cleaned.jsonl
echr_cases_scraped.jsonl
docs/data/echr_cases.jsonl
docs/data/echr_cases_enriched*.jsonl
docs/data/*.bak.*
# NOTE: docs/data/stats.json and docs/data/echr_cases_sample50.jsonl ARE tracked — the live site needs them

# ───────────────────────── RAG artifacts (rebuildable; 5 GB+) ─────────────────────────
rag/**/data/
*.index
*.f32.dat
*.faiss
citations.json
# 10 GB local research tree — code lives in rag/, raw artifacts stay out
benchmark/

# ───────────────────────── Local tooling / scratch ─────────────────────────
.claude/
claude-design-idea/
mysite/
*.docx

# legacy Flask scrapers (kept local only)
legacy/echr_dashboard/scraper_hudoc.py
legacy/echr_dashboard/scraper_chrome.html
legacy/echr_dashboard/analysis_selimi.js

# private internal audits — never push
notes-internal/*audit*.md
notes-internal/*-audit-*.md
notes-internal/*audit*.json
notes-internal/*audit*.html

# AUEB-related scripts — researcher-private
scripts/p27_aueb*.py
scripts/*_aueb_*.py
scripts/*aueb*.py

# internal LLM-judge artefacts (raw paragraph text + working notes)
scripts/_judge_internal/
scripts/_judge_internal/**
scripts/p52_judge_segmentation.py
scripts/p52_judge_sample.txt
echr_judge_runs/

# local HUDOC reference HTML snapshots (side-by-side compare; not shipped)
docs/*_hudoc.html
